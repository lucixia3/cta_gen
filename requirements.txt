# Dependencies to run the diffusion generator (pip install -r requirements.txt).
# Python 3.10+.
numpy
scipy
nibabel
scikit-image
torch                # CUDA GPU required for sampling and training
monai                # SPADEDiffusionModelUNet + schedulers
huggingface_hub      # download the pretrained weights (lborrego/gen_cta)
